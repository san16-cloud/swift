startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({
      command: ["npm", "start"],
      cwd: "/app/mcp-service"
    })
build:
  dockerfile: mcp-service/Dockerfile
  dockerBuildPath: mcp-service
